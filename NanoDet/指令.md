# 调用摄像头实时检测
python demo/demo.py webcam --config task.yml --model weights/task.ckpt --camid 1    


# 导出 ONNX 模型

python tools/export_onnx.py --cfg_path task.yml --model_path weights/task.ckpt

# 训练
python tools/train.py task.yml

# 1. 创建并激活虚拟环境
conda create -n nanodet python=3.8 -y
conda activate nanodet

# 2. 安装PyTorch（适配CUDA11.1）
conda install pytorch torchvision cudatoolkit=11.1 -c pytorch -c conda-forge

# 3. 克隆项目
git clone https://github.com/RangiLyu/nanodet.git
cd nanodet

# 4. 安装依赖
pip install -r requirements.txt

# 5. 安装项目（开发模式）
python setup.py develop

# 检测单张图片
python demo/demo.py image --config config/nanodet-plus-m_416.yml --model 你的模型路径.ckpt --path 图片路径.jpg

# 检测视频
python demo/demo.py video --config 配置文件.yml --model 模型路径.ckpt --path 视频路径.mp4


